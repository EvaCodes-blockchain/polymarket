{
  "name": "generator",
  "version": "0.1.0",
  "private": true,
  "description": "PolyMarket Social — news-driven market generator (test/demo component)",
  "scripts": {
    "generate": "tsx src/index.ts --once",
    "generate:loop": "tsx src/index.ts --loop",
    "trade": "tsx src/trading/index.ts --once",
    "trade:loop": "tsx src/trading/index.ts --loop",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "fast-xml-parser": "^4.5.3",
    "viem": "^2.28.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3",
    "vitest": "^3.1.4"
  }
}
