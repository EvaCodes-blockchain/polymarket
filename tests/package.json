{
  "name": "tests",
  "version": "0.1.0",
  "private": true,
  "description": "PolyMarket Social — API integration tests (run against the live local stack)",
  "scripts": {
    "test": "vitest run --config vitest.config.ts",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22",
    "@types/pg": "^8.11.10",
    "pg": "^8.13.1",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3",
    "viem": "^2.28.0",
    "vitest": "^3.1.4"
  }
}
